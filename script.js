//set onclick event to blocks
window.onload = function () {
    var anchors = document.getElementsByClassName('sBlock');
    for (var i = 0; i < anchors.length; i++) {
        var anchor = anchors[i];
        anchor.onclick = function () {
            //alert('Hey, button is work!');
            //this.setAttribute("style", "height:500px");
            this.
            //this.emit('eventName');
        }
    }
}